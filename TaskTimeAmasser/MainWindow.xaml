<Window x:Class="TaskTimeAmasser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:bh="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:local="clr-namespace:TaskTimeAmasser"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="MainWindow" Height="618.75" Width="973.75">
    <bh:Interaction.Triggers>
        <bh:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction />
        </bh:EventTrigger>
    </bh:Interaction.Triggers>
    <Grid>
        <Grid Height="104" VerticalAlignment="Top" Background="#FFFFF1F1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Grid.Column="0" Margin="10,0" Content="{materialDesign:PackIcon Database}" />
            <TextBox Grid.Row="0" Grid.Column="1"  TextWrapping="NoWrap" Text="{Binding DBFilePath.Value}"  FontSize="14" Margin="0,10,0,10" Padding="10,5,0,0" materialDesign:TextFieldAssist.PrefixText="DBファイル"/>
            <Button Grid.Row="0" Grid.Column="2" Margin="10,10" />
            <Button Grid.Row="1" Grid.Column="0" Margin="10,0" Content="{materialDesign:PackIcon Folder}" />
            <TextBox Grid.Row="1" Grid.Column="1" TextWrapping="NoWrap" Text="{Binding LogDirPath.Value}"  FontSize="14" Margin="0,10,0,10" Padding="10,5,0,0" materialDesign:ThemeAssist.Theme="Light" materialDesign:TextFieldAssist.PrefixText="ログフォルダ" />
            <Button Grid.Row="1" Grid.Column="2" Content="{Binding LogDirLoad.Value}" Margin="10,10" />
        </Grid>
        <Grid Background="#FFD6EAFB" Margin="0,104,0,0">
            <DataGrid ItemsSource="{Binding DB.Value}" IsReadOnly="True" Margin="258,10,10,10">
                <!--
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        
                    </Style>
                </DataGrid.CellStyle>
                -->
            </DataGrid>
        </Grid>

    </Grid>
</Window>
